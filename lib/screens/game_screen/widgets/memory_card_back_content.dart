
import 'package:flutter/material.dart';

class MemoryCardBackContent extends StatelessWidget {
  const MemoryCardBackContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.sign_language);
  }
}